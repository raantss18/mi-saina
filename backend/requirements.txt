fastapi>=0.100.0
uvicorn>=0.23.0
httpx>=0.25.0
ddgs>=9.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0
ollama>=0.1.0
aiofiles>=23.0.0
websockets>=11.0
rich>=13.0.0
sqlalchemy>=2.0.0
aiosqlite>=0.19.0
# [mi-saina-improve] sentence-transformers RETIRÉ : dépendance morte (jamais importée)
# qui tirait torch (~750 Mo–2 Go) inutilement. Les embeddings passent par Ollama
# (/api/embeddings, nomic-embed-text). Allège l'install et rend un AppImage autonome
# réaliste. numpy reste requis (similarité cosinus en mémoire).
numpy>=1.24.0

# Lecture de documents (PDF/Word/Excel/PowerPoint) — pur-python, léger
pypdf>=4.0.0
python-docx>=1.1.0
openpyxl>=3.1.0
python-pptx>=0.6.23

# Serveur MCP « fetch » (résumé/lecture de pages web) — configuré automatiquement
mcp-server-fetch>=2025.1.17

# Testing
pytest>=7.4.0
pytest-asyncio>=0.23.0
pytest-cov>=4.1.0
respx>=0.20.0
